import csv_dataset
select from products Table all position
select onle name and phone number from shippers Table
select min and max price
sorted 10 unic data
select product for price 20-100
select count and average price from to supplier
